# BREADCRUMB: server > storage - two-tier persistence: sqlite hot + parquet archive (compactor, hash manifest). Contract: 0004,0006,0009. Parent: server/AGENTS.md.
# REUSE-WHEN: you're reading/writing device readings — go through the two-tier writer, don't touch the DBs directly
