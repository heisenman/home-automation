# Copy to secrets.yaml (gitignored, off-git) and fill in real values before building.
# Same WiFi/OTA credentials the rest of the ESPHome fleet uses (see provisioning/levoit/).
wifi_ssid: "CTWap_24g"
wifi_password: "REPLACE_ME"
ota_password: "REPLACE_ME"
fallback_ap_password: "REPLACE_ME"
