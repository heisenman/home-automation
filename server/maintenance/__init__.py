# BREADCRUMB: server > maintenance - idempotent device rename/relocate/retire across EVERY store on both boxes (registry/hot/parquet/rungs/ntfy). Contract: 0016,0022,0026,0027. Parent: server/AGENTS.md.
# REUSE-WHEN: you're renaming/relocating/retiring a device — reuse device_migrate/relocate/placement, never hand-edit a registry
