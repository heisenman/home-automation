# ESPHome secrets for a Levoit purifier conversion. Copy to secrets.yaml (NOT in git) and fill in.
wifi_ssid: "CTWap_24g"                 # case-sensitive
wifi_password: "REPLACE_ME"
ota_password: "REPLACE_ME"             # generate: openssl rand -hex 16  (save it — needed for every OTA)
fallback_ap_password: "REPLACE_ME"     # >=8 chars; the recovery-hotspot password
