# BREADCRUMB: server > util - shared cross-cutting helpers (MQTT credentials, registry mtime-reload). Contract: -. Parent: server/AGENTS.md.
# REUSE-WHEN: you need a cross-cutting helper (mqtt creds, registry reload) — reuse before reimplementing
