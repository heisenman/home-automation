# Example per-device command secrets. Copy to instance/control_secrets.yaml (GITIGNORED) and fill
# with real values. Each must match the HMAC secret provisioned onto the device (console enrollment,
# physical presence — plan §13.3). Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
lamp_office: "REPLACE_WITH_32_BYTE_HEX_SECRET"
lock_front: "REPLACE_WITH_32_BYTE_HEX_SECRET"
thermostat_main: "REPLACE_WITH_32_BYTE_HEX_SECRET"
