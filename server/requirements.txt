# Home automation server dependencies
# Pin minor versions; update deliberately (per §17.5 — dependency maintenance item)

bleak==0.22.3
paho-mqtt==2.1.0
PyYAML==6.0.2
pyarrow==18.1.0   # 18.x is the first release with cp313 wheels (Debian 13 / Py3.13); 17.x was cp312-only
duckdb==1.1.3
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.1
cryptography==49.0.0   # control plane: Fernet-encrypted node-secret LUT (server/control/secret_store.py)
