# BREADCRUMB: server > ingest - MQTT/BLE to canonical readings; device-family bridges (tasmota/levoit) + edge-mapper (MAC to device/area) + registry live-reload. Contract: 0001,0023. Parent: server/AGENTS.md.
# REUSE-WHEN: you're turning device telemetry into canonical readings or adding a device family — reuse a bridge + the registry reloader
